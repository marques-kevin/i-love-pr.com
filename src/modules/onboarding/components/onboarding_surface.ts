/** Hairline ring (~10% ink) — landing command boxes and tiles. */
export const landing_hairline =
  'shadow-[0_0_0_1px_color-mix(in_oklch,var(--color-base-content)_10%,transparent)]'

export const landing_command_box = `bg-base-200 flex items-center overflow-hidden rounded-[1rem] ${landing_hairline}`
